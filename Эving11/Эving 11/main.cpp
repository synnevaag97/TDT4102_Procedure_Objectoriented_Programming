
#include "Iteratorer.h"
#include "List.h"
#include "Templates.h"
int main(){
    // std::set<std::string> names{"Trym",
    //                                 "Per",
    //                                 "Albert",
    //                                 "Åse"};
    // loopSet(names);
    // replaceSet(names, "Trym", "Håvard");
    // loopSet(names);
    // 

    // std::list<Person> listPerson;
    // Person p1{"Trym", "Synnevåg"};
    // Person p2{"Håvard", "Synnevåg"};
    // Person p3{"Sondre", "Synnevåg"};
    // Person p4{"Ida", "Synnevåg"};

    // insertOrdered(listPerson,p1);
    // insertOrdered(listPerson,p2);
    // insertOrdered(listPerson,p3);
    // insertOrdered(listPerson,p4);
    // for(const auto& p : listPerson){
    //     std::cout << p << '\n';
    // }
    test_template();
}